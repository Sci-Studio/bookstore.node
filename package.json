{
  "name": "bookstore..node",
  "version": "1.0.0",
  "description": "API and Backend for demo bookstore",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon --watch src --exec \"npx ts-node --project tsconfig.json src/index.ts\"",
    "build": "prisma generate && tsc",
    "start": "node dist/src/index.js",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "seed": "npm run generate && ts-node scripts/seed.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sci-Studio/bookstore..node.git"
  },
  "author": "Hisham Saeed",
  "license": "GPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/Sci-Studio/bookstore..node/issues"
  },
  "homepage": "https://github.com/Sci-Studio/bookstore..node#readme",
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^24.9.2",
    "nodemon": "^3.1.10",
    "prisma": "^6.18.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
