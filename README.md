# bookstore.node
This the backend program using nodejs

# To Run Project for first time
- Clone the project through ssh, make sure you have an ssh key to link repository with local machine
- copy .env file to your root directory
- make sure node version 20 is used or higher
- run 
```
npm install
npm run generate
npm run dev
```